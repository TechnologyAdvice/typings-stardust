/// <reference path="../stardust.d.ts" />
import * as React from 'react'
import {
  Button,
  Container,
  Divider,
} from '../stardust'

const renderButton = () => (
  <Button className='hi' />
)

const renderContainer = () => (
  <Container textAlign='center' />
)

const renderDivider = () => (
  <Divider />
)